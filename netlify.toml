[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=optional"

[functions]
  # Generation rasterizes + traces several designs.
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"
